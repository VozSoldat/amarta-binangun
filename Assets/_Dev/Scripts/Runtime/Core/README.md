# Kegunaan Folder `Core`

Folder `Core` digunakan untuk menyimpan kode yang digunakan dalam domain, sistem, atau prefab tertentu (specialized). Misalnya `EntityMovement` dan konkretnya yakni `PlayerMovement` secara berurutan dapat digunakan pada Entity dan Player.